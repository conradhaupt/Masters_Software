# Used to test compiler features at configure
add_subdirectory(compiler_features)

# Used to test the units of the library
add_subdirectory(unit_tests)

# Used to benchmark functionality of the library
add_subdirectory(benchmark_tests)
